# Field Sales Visit App

GPS-based field sales attendance and outlet visit tracker.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000. GPS access requires a secure context such as HTTPS or localhost.
